array(陣列)

element(元素)

index(索引)

length(長度)

one-dimension、one-rank(一維)

multi-dimension、multi-rank(多維)

jagged array(不規則陣列)

//=================================================
System.Array 類別
Public Property(公有屬性)

IsFixedSize  //取得表示陣列是否為固定大小的布林值，通常為 true

IsReadOnly  //取得表示陣列是否為唯讀的布林值，通常為 false

Public Method(公有方法)
  BinarySearch(arr,obj)  //使用二元搜尋法在 arr 陣列內搜尋 obj 所在位置的索引，傳回值為 int 型別。
